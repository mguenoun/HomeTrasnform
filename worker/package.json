{
  "name": "hometransform-notifications-worker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "generate-vapid-keys": "node --input-type=module -e \"import webpush from 'web-push'; console.log(webpush.generateVAPIDKeys());\""
  },
  "dependencies": {
    "jose": "^6.1.0",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260911.1",
    "@types/web-push": "^3.6.4",
    "typescript": "~5.9.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.42.0"
  }
}
